import React from "react";
import { StyleSheet, Text, View } from "react-native";
import { Ar } from "../ArModule";

export default function ItemList() {
    return (
      <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
        <Text>Items!</Text>
      </View>
    );
  }